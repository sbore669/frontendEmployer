// Type definitions for Electron API
declare interface Window {
  electronAPI: {
    // Définissez ici les méthodes exposées par le préchargement
    // Par exemple:
    // sendMessage: (message: string) => void;
    // onResponse: (callback: (event: any, response: any) => void) => void;
  }
}